# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# Source PNGs kept only for regenerating optimized WebP via scripts/optimize-images.mjs.
# These are large (~77MB) and never served (the build ships only dist/ WebP) — keep out of git.
image-originals/

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Re-include the documentation SITE source. The repo-root .gitignore has a
# generic `docs/` rule (intended for downloaded API docs) that would otherwise
# exclude these — they are first-class source for the docs site and must ship.
!src/content/docs/
!src/content/docs/**
!src/components/docs/
!src/components/docs/**
!src/pages/docs/
!src/pages/docs/**
